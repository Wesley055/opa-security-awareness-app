import {render,screen} from '@testing-library/react';import {expect,it} from 'vitest';
import {DeliveryConfirmation} from './delivery-confirmation';import {ReportingShell} from './reporting-shell';import type {DeliveryAttempt} from '@/lib/delivery-confirmation';
const row:DeliveryAttempt={id:'1',channel:'SMS',recipientMasked:'***1234',state:'DELIVERED',providerReference:'ref-1',failureClass:null,attemptCount:2,queuedAt:'2026-09-10T01:00:00Z',acceptedAt:'2026-09-10T01:00:01Z',deliveredAt:'2026-09-10T01:00:02Z',failedAt:null,updatedAt:'2026-09-10T01:00:02Z',deliveryProof:null};
it('withholds a delivered claim when proof is missing',()=>{render(<DeliveryConfirmation snapshot={{state:'READY',incidentId:'i',asOf:'now',attempts:[row],nextCursor:null}}/>);expect(screen.getByText('Status: Delivery proof unavailable')).toBeVisible();expect(screen.queryByText(row.deliveredAt!)).toBeNull();});
it('renders proven delivery with its provider reference and timestamp',()=>{render(<DeliveryConfirmation snapshot={{state:'READY',incidentId:'i',asOf:'now',attempts:[{...row,deliveryProof:{source:'provider-webhook',receivedAt:'2026-09-10'}}],nextCursor:null}}/>);expect(screen.getByText('Status: DELIVERED')).toBeVisible();expect(screen.getByText('ref-1')).toBeVisible();expect(screen.getByText(row.deliveredAt!)).toBeVisible();});
it('presents the missing delivery reader as unavailable',()=>{render(<DeliveryConfirmation/>);expect(screen.getByRole('status')).toHaveTextContent('capability pending');expect(screen.queryByText('No notification attempts are recorded.')).toBeNull();});
it('provides four reporting areas without fabricated data',()=>{render(<ReportingShell/>);for(const name of ['Reports','After-Incident Reports','Aggregate Analytics','Corrective Actions'])expect(screen.getByRole('heading',{name})).toBeVisible();expect(screen.getAllByRole('status')).toHaveLength(4);for(const status of screen.getAllByRole('status'))expect(status).toHaveTextContent('Not enabled / backend capability pending');});
