import Link from 'next/link';
const areas=[['Reports','Approved institutional reports and their publication status.'],['After-Incident Reports','Incident chronology, evidence references and confirmed outcomes.'],['Aggregate Analytics','Authorized aggregate measures with source coverage and time windows.'],['Corrective Actions','Assigned follow-up actions, ownership and completion history.']] as const;
export function ReportingShell(){return <section className="mx-auto max-w-7xl p-4 sm:p-6"><h1 className="text-3xl font-bold">Reports / Analytics</h1><nav aria-label="Reporting areas" className="mt-4 flex flex-wrap gap-2">{areas.map(([title],index)=><Link key={title} href={'#report-area-'+index} className="inline-flex min-h-11 items-center rounded-md border border-line px-3">{title}</Link>)}</nav><div className="mt-6 grid gap-4 lg:grid-cols-2">{areas.map(([title,description],index)=><section id={'report-area-'+index} key={title} className="scroll-mt-40 rounded-xl border border-line bg-panel p-5"><h2 className="text-xl font-bold">{title}</h2><p className="mt-2 text-sm text-muted">{description}</p><p role="status" className="mt-4">{index === 2 ? 'See OPA Insight below for facility metric availability.' : 'Controls for this area are not available in this view yet.'}</p></section>)}</div></section>;}
