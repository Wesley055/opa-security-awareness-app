# OPA - Handover Ultra 19 - PART 2 of 3 (sections 9b-14)
# Continues from part 1. Paste after it.
#
# GENERATED: 2026-08-01 (local), at commit 3e9b703 - UNPUSHED.

## 9b. WHAT THIS SESSION ESTABLISHED

### 9b.1 `/health/ready` RETURNS 503 IN PRODUCTION - THE FULL EVIDENCE CHAIN

**Every link measured. This is the section to quote about the readiness defect.**

**1. The symptom, from outside Azure.**
```
resp_status=503
```
An `Invoke-WebRequest` probe returned the status and an **EMPTY body** - the
exception stream gave nothing. **That was ambiguous**: an empty-bodied 503 is
also what an Azure front-end 503 looks like, which would mean the request never
reached Nest and everything reasoned from the code would be beside the point.
Trap #102.

**2. The disambiguation.** `curl.exe -s -i`:
```
HTTP/1.1 503 Service Unavailable
Content-Length: 91
Content-Type: application/json; charset=utf-8
Access-Control-Allow-Credentials: true
Vary: Origin
X-Powered-By: Express
x-correlation-id: 8ef902c2-13c0-4b37-a191-b367c6e6b3db
{"status":"degraded","database":"up","redis":"down","timestamp":"2026-08-01T21:35:57.985Z"}
```

**`X-Powered-By: Express` and an `x-correlation-id` prove this went through
`RequestLoggingMiddleware` and out of `HealthController`. The 503 is the
application's own.**

**3. The chain, in code.**
```
health.service.ts:40-42  redis = (await this.redis.isHealthy()) ? 'up' : 'down'
health.service.ts:44     allUp = database === 'up' && redis === 'up'
health.service.ts:47     status: allUp ? 'ok' : 'degraded'
health.controller.ts:22-24   res.status(result.status === 'ok' ? 200 : 503)
```
Redis is down by design, so `allUp` is false, so `degraded`, so **503**.

**4. Why nothing is affected.** Azure Portal -> `opa-api-production` ->
Monitoring -> Health check: **enabled, probe path `/health`.** `/health` is
`getLiveness()`, which touches no dependency and carries an explicit
`@HttpCode(HttpStatus.OK)`. **Azure has been probing the safe route all along.**

**5. Why it still matters.** The same blade configures **unhealthy instance
removal with a 10-minute load-balancing threshold**, and its own text says an
instance that stays unhealthy will be replaced. **If the probe path were ever
moved to `/health/ready`, Azure would cycle a healthy container on a permanent
503, and it would present as an application crash.** The portal shows
**"Health check is being moved to Configuration"** - a UI migration is exactly
the event during which a setting gets re-entered by hand.

**6. THE FREE FACT THAT CAME WITH IT.** `"database":"up"` is the result of
`health.service.ts:34`'s `` await this.prisma.$queryRaw`SELECT 1` `` executed by
the **deployed application**. Every prior database claim in this project came
from the isolated Prisma CLI at `/home/prisma-tools` over SSH. **This is the
first evidence that `DATABASE_URL` as the App Service hands it to the running
Nest process is correct and that the connection works from inside the app.**
**It is a READ. Issue 17.27 - no production WRITE path has ever executed -
still stands.**

### 9b.2 q47 ANSWERED - WHAT ACTUALLY DEPENDS ON REDIS

**One consumer. `health.service.ts`. That is all.**

A case-insensitive sweep of `apps/api/src` returned **38 hits against
`scanned_ts=120`**, with `import_control_hits=484` as the unconditional control.
Partitioned:

- **28** are Redis defining itself (22 service, 4 module, 2 `app.module.ts`).
- **6** are `health.service.ts`.
- **3** are `redisDispatchPrepared` (service `:32`, spec x2) - a placeholder
  `TODO.md:374-375` says nothing reads.
- **1** is `env.validation.ts:7`.

**`RedisService.getClient()` at `redis.service.ts:76` HAS NO CALLERS.** The 38
hits are exhaustive and none is a call site.

**THE FIRST ANSWER WAS `hits=0` AND IT WAS A FALSE NEGATIVE.** Had it been
believed, the session would have concluded that nothing depends on Redis and
closed q47 wrongly - a conclusion that would also have contradicted the
production log, which shows `ERROR [RedisService]`, a NestJS logger context,
meaning the class is instantiated and running. **Something that logs must be
defined somewhere.** Trap #101.

**`TODO.md:352-381` already recorded the deferral.** Part 1 section 3 has the
full window. The operative lines: provision only when a feature needs Redis
(`:355-357`); no code change is needed, only the `REDIS_URL` value (`:360-361`);
verify `/health/ready` shows `redis: up` afterwards (`:362-363`); the dispatch
pass is the work that USES Redis and is unstarted (`:370-375`).

**q48 - does an Azure Cache instance exist - is NARROWED, NOT CLOSED.** It is not
needed to explain current behaviour. **It should still be checked before any
future provisioning, to avoid standing up duplicate infrastructure.**

### 9b.3 THE `REDIS_URL` CORRECTION - AND WHY IT MATTERS MORE THAN THE FACT

**Ultra 18 states in three places that `REDIS_URL` is "literally the string
`placeholder`."** The portal shows **`redis://placeholder:6379`**.

The only evidence was `getaddrinfo ENOTFOUND placeholder`, which constrains the
**hostname** and says nothing about the surrounding string. `redis://placeholder:6379`
fits that observation perfectly, passes `z.string().url()`, and boots.

**A whole finding was built on the wrong reading.** This session concluded, for
about an hour, that `env.validation.ts:7`'s `z.string().url()` could not be
enforcing - since `placeholder` is not a URL - and that a schema claiming to
guarantee a URL was not guaranteeing it. **That would have been a serious
finding. It was false.** The validator works correctly.

**The shape is worse than trap #96's.** A zero from a dead probe looks like a
true zero and is caught by a control. **This was an observation consistent with
several causes, where one cause was written down as fact and then reasoned from
a session later.** The handover's own "verify, do not predict" rule did not
catch it, because the claim arrived already looking verified.

**Nobody types `redis://placeholder:6379` by accident.** The scheme and the
canonical Redis port confirm the deliberate-stub reading beyond doubt.

**Replacement statement for any live document:**
> `REDIS_URL` is configured as `redis://placeholder:6379`, a syntactically valid
> Redis URL whose hostname is intentionally unresolved while Redis-backed
> features remain deferred.

### 9b.4 q43 CLOSED - FOUR INDEPENDENT WAYS

**Does the orchestrator tolerate FULLY NULL enrichment? YES.** Production runs
right now with all six intelligence sections suppressed, so this was the state a
real SOS would hit.

**1. The orchestrator never dereferences it.** Ten `intelligence` occurrences in
457 lines, measured with `control_await_hits=18`. After the assignment at
`:85-98` the only uses are two COMMENTS (`:153-156`, `:257`) and two bare
shorthand properties in return statements (`:356` retrigger, `:422` activation).
**No field access exists to fail.**

**2. `:74` already returns a fully null `intelligence`** on the NOT_ACTIVATED
path, so the return shape has always tolerated it.

**3. The enrichment object is structurally safe by construction.**
`emergency-intelligence.service.ts` read in full: `location` (`:90-104`) and
`emergencyResources` (`:125-132`) are **ALWAYS objects**; `latitude`,
`longitude` and `accuracy` **survive suppression**; `movement` and `device` are
**never suppressed**. Only `surroundings` and `routes` become null wholesale.
**A consumer reaching for `intelligence.location.latitude` gets a real number.**

**4. `buildLocationIntelligence` cannot throw.** No `throw` in the method; every
mock check is a ternary; the six providers cannot reject (9b.5). It always
reaches the return at `:147`.

**WHAT REMAINS OPEN, NARROWLY:** `surroundings` and `routes` are the only
null-able containers. **Whether anything downstream - a notification template, a
response builder - dereferences `intelligence.surroundings.places` or
`intelligence.routes[0]` is unmeasured.** That is a much smaller question than
"does the notification path handle null enrichment," and it is the honest
residue of q43.

### 9b.5 THE PROVIDERS HAVE NO NETWORK LAYER AT ALL

**All seven read in full, 466 lines.** A sweep for
`throw|reject|fetch|axios|http|timeout|catch` returned **`hits=0` in every one**,
each file printing its own line count, so the seven zeros are true zeros.

**Every method is a bare `return` of a literal.** No branching, no computation
that can fail, not even an array index.

**TWO CONSEQUENCES, AND THE SECOND IS A SPRINT-SIZING FACT:**

**1. `Promise.all` at `:50` cannot fail today.** The fail-fast exposure is
**LATENT**, not a present defect.

**2. Sprint 10C is not "swap mock data for real data."** There is no HTTP client,
no retry, no timeout, no error handling to adapt. **The integration layer is
built from nothing.**

**THE LATENT ISSUE, STATED PRECISELY.** ADR-012 changed **what** is returned -
mock-backed sections are suppressed - but not **how** providers are orchestrated.
**The suppression contract at `:86-157` begins only after `:50`'s `Promise.all`
completes successfully.** If a future production provider performs network I/O
and rejects, `Promise.all` rejects, the exception propagates out of
`buildLocationIntelligence`, out of the orchestrator's **unguarded** `await` at
`:86`, and kills the SOS activation. **A network blip at one provider would take
down the incident.**

**AND NOTHING WOULD NOTICE.** A case-insensitive sweep of the 697-line
`emergency-intelligence.service.spec.ts` for `reject|throw|failure|failed|error`
returns **four hits, ALL comments** (`:49`, `:51`, `:316`, `:675`). **The
suppression contract is thoroughly pinned; the orchestration is not pinned at
all.**

**`Promise.allSettled` is the obvious shape** - it matches what the file already
does philosophically, since a section that cannot be trusted becomes null rather
than absent. A rejected real provider is the same situation as a mock provider.
**But this is a DECISION, and it belongs in an ADR written BEFORE 10C wires
anything, because afterwards the safe refactor competes with a working
integration.**

**FOUR REGRESSION ASSERTIONS THAT ADR MUST REQUIRE:**
- one provider failure still produces an SOS;
- failed provider sections become `null`;
- healthy providers still contribute their data;
- **device GPS is ALWAYS returned.** *(Load-bearing: `location.latitude`,
  `longitude` and `accuracy` survive every suppression path today, and a
  fault-isolation rewrite must not quietly make the whole `location` object
  nullable when one provider dies. That property is what makes an SOS useful with
  zero enrichment.)*

**TWO PROPERTIES OF THE MOCK DATA WORTH RECORDING.** It is **Ikeja-anchored and
coordinate-relative** - every provider offsets from the caller's position while
the names are fixed Lagos institutions, so an SOS from anywhere returns "Lagos
State University Teaching Hospital" 2.8km north with a fabricated distance. And
`places.provider.ts` and `safe-place.provider.ts` set **`isVerified: true` on
mock records.** If suppression ever failed, fabricated locations would arrive
flagged as verified. **Both make ADR-012's suppression layer's load-bearing
status concrete rather than abstract.**

### 9b.6 ADR-016 - HOW THE DECISION WAS REACHED

**`fc3d72c`, 206 lines, `209 0`, pushed.** Full decision text in part 1
section 5. What is worth preserving is the reasoning, and the four things that
were nearly recorded wrongly.

**1. "Nothing is currently affected" was too broad.** `/health/ready` IS
affected, and Redis generates reconnect noise. **What is unaffected is App
Service availability.** The ADR says so precisely.

**2. The Swagger summary proves inconsistency, NOT intent.** A draft said
`health.controller.ts:19` "already describes the intended behaviour." It does
not - it establishes that prose and code disagree. **And `44ff065`'s subject,
`Add Redis integration: global module, connection lifecycle, readiness health
check`, shows the readiness coupling shipped WITH the Redis foundation.** It was
written for a Redis expected imminently; the expectation changed and the check
did not. **That is context for the commit message, not a decision.**

**3. "Readiness is not a dependency inventory" was imprecise.** The response
still REPORTS optional infrastructure. **What must not be an inventory fold is
the VERDICT.** The ADR's title carried the same imprecision and was rewritten.

**4. D4 needed a policy LOCATION, not just a principle.** Saying optionality must
be explicit is insufficient without saying where it lives. **The required-
dependency set is declared in application code, and `REDIS_REQUIRED=false` is
named and rejected**, because an env toggle would make a reviewed architectural
statement editable from a portal blade - recreating the exact drift the decision
prevents.

**5. D6 could not cite a handover as a policy location.** A draft pinned the
probe path in "this ADR, `TODO.md`, and the operations note owed under Ultra 18."
**Handovers are superseded.** Replaced with a permanent production-operations
runbook, plus a check during deployment verification.

**Section 4 - "What this does not decide" - exists to prevent a specific
failure.** It names the `Promise.all` case and the ingestion batch case (17.22,
q40) and explicitly leaves them open. **Without it, a future session finds a
degradation-flavoured ADR and reasonably assumes enrichment was covered.**

### 9b.7 THE SPECS - `3e9b703`, HELD UNPUSHED

**Two new files in a module that had ZERO tests. 110 insertions.**

**The suite state, measured:**
```
Test Suites: 1 failed, 1 passed, 2 total
Tests:       1 failed, 1 todo, 3 passed, 5 total
Time:        12.016 s
```
```
HealthService > reports optional-down and stays ok when Redis is unavailable
  Expected: "optional-down"
  Received: "down"
  at src/modules/health/health.service.spec.ts:24:24
```

**THAT FAILURE IS ADR-016 D3 STATED AS AN EXECUTABLE ASSERTION.**

**The controller suite PASSED**, which locates the defect precisely: **the
verdict is wrong, the status-code mapping is not.** It also confirms a fresh
controller spec booting a real Nest app surfaces no hidden wiring here.

**What each test does and why it was chosen:**

| Test | Now | Purpose |
|---|---|---|
| optional-down, status ok | **FAILS** | the specification |
| database down -> degraded | passes | **regression guard** - widening the vocabulary must not weaken the check |
| controller ok -> 200 | passes | pins the mapping independently of the verdict |
| controller degraded -> 503 | passes | same |
| Redis required and down -> 503 | **`it.todo`** | ADR-016 D5 |

**ONLY ONE OF THE FOUR WAS EXPECTED TO FAIL, AND KNOWING THAT IN ADVANCE
MATTERED.** A session expecting four red tests would have concluded something was
wrong when three passed.

**THE `it.todo` IS DELIBERATE AND ITS WORDING IS EXACT:**
```ts
it.todo(
  'reports Redis as down and readiness as degraded when Redis is required but unavailable',
);
```
D5's graduation case **cannot be written today** - there is no required-dependency
set to mark Redis into, and inventing a temporary API purely for a test was
rejected. **`it.todo` keeps the requirement visible in jest output rather than
only in the ADR.** **The implementation commit must replace it with a real
passing test, and `todo=0` is that commit's gate.**

**A TYPESCRIPT DETAIL WORTH KEEPING.** `ReadinessStatus.redis` is currently
`'up' | 'down'`, so asserting `'optional-down'` risks a compile error, which
would stop the suite running rather than fail a test. **Widening through an
intermediate avoids any cast:**
```ts
const redisState: string = result.redis;
expect(redisState).toBe('optional-down');
```
**`tsc --noEmit` exit 0 with this in place, measured first try.**

**WHY THE COMMIT IS HELD.** It makes `main` red. **CI has no test gate**, so
nothing would block a deploy, and a red `main` on origin - with no gate to
explain it - is a state another session would reasonably read as a broken build.
**Two commits pushed together tell a coherent story; one pushed alone does not.**

### 9b.8 THE INSTALLER DISCIPLINE - 38 FOR 38, WITH TWO NEW RULES EARNED

**Two installers. Neither needed a retry against the real repository.** Ten
replica cases for the ADR, seven for the specs, the latter re-run **in full**
after each of three revisions.

**1. AN IDEMPOTENCY SENTINEL MUST EXIST ON A SINGLE LINE.** Trap #103. The ADR
installer's first sentinels were prose sentences; the ADR is hard-wrapped at ~78
characters, so neither existed on any line. **Phase 1 said "fresh install"
correctly by accident; phase 2's post-write sentinel count returned 0,0 and
failed the clean run.** **The guard would have permitted a SECOND insertion of the
whole 206-line ADR.** Replaced with the `## ADR-016 - ...` heading and the
`redis: 'up' | 'down' | 'optional-down';` code line, both verified unique by
count. **The failure happened in the replica, which is the whole argument for
replica testing.**

**2. AN INSTALLER'S PRINTED "EXPECTED" RESULT IS NOT AN ACCEPTANCE CRITERION.**
The spec installer originally printed `expect 1 failed, 3 passed, 1 todo`. That
is a **prediction** about code never executed. Reworded to name the expected
PRIMARY failure and to state that **any additional failure must be investigated
before production code changes** - a new controller spec can surface global
pipes, middleware or module wiring unrelated to the decision. Same family as
trap #85.

**3. CREATE-ONLY INSTALLERS MUST REFUSE TO OVERWRITE A DIVERGENT FILE.**
Identical -> no-op. Absent -> create. **Present and different -> abort with a
diagnosis, naming the character counts.** Exercised in the replica.

**4. TWO-PHASE IS LOAD-BEARING FOR MULTI-FILE CREATION.** With one target present
and divergent, phase 1 refused **both** files - `controller_created=0`. **A
single-phase installer would have created the other and left a half-applied
state.**

**5. RECOVERY INSTRUCTIONS MUST MATCH THE FILE'S GIT STATE.** The ADR installer
prints `git checkout -- docs/architecture/decision-log.md`, correct for a tracked
file. **The spec installer prints explicit `del` commands, because files it
creates are UNTRACKED until committed and `git checkout` will not remove them.**

**6. MEASURE THE TARGET DIRECTORY'S EOL AND BOM FROM A NEIGHBOUR AND MATCH IT.**
The spec installer read `health.service.ts`, measured `eol=LF bom=false`, and
wrote LF. **Against a CRLF replica it wrote CRLF instead.** It also **aborts if
the neighbour carries a BOM**, rather than guessing.

**7. PHASE 2 MUST BE ABLE TO FAIL - PROVEN AGAIN.** Both installers were run
against a stubbed `fs.writeFileSync` substituting `optional-down` ->
`optionaI-down` (capital I, **same length, so a byte gate passes**).
`written === plannedContent` caught both and exited 1.

**8. A TEST HARNESS CAN PRODUCE A FALSE PASS.** Trap #104. The BOM case appeared
to pass because this shell's `printf` does not interpret `\xEF`; the fixture had
no BOM. Re-run with octal `\357\273\277`, the guard fired. **Also: `$?` after a
pipe reports grep's status, not the program's** - several case results had to be
re-measured by running the installer with output discarded and reading the true
exit code.

**9. AN OBSERVATION IS NOT A GATE - EXERCISED.** The ADR installer treats a
changed target line count as a NOTE. A 1002-line replica installed correctly with
the note printed. **Violating it does not make the edit unsafe, because the
insertion anchors on content.**

**10. EVERY REVISION CHANGES THE BYTE COUNT.** The spec installer went
11059 -> 11535 -> 11552. **The placement gate correctly refused the superseded
copies**, and on one occasion refused a run entirely (`files=0`) because a
double-paste had fused two commands. Trap #100.

### 9b.9 ADR-014 - THE ENDED-SESSION FIX CONTRACT. FULL CONTENT, DECIDED, STILL OWED.

**Status when written: Accepted (design), Not Implemented. UNCHANGED for two
sessions.** Carried forward verbatim in substance from Ultra 18.

**THE PROBLEM.** `journey-ingestion.service.ts:107` throws
`ConflictException('Journey session has ended.')` for **any** fix addressed to an
`ENDED` session, before examining a single fix. A batch buffered across a
supersession is rejected **entirely**, including fixes captured while the session
was still open. **Item 10's airplane-mode test is exactly this scenario.**

**THE DECIDED CONTRACT.** A fix submitted against an `ENDED` session is:
- **Accepted** when `recordedAt < endedAt`
- **Permanently rejected against that session** when `recordedAt >= endedAt`
- **Retried** only for transient infrastructure failures

**Permanently invalid for this session** - a specific client error with a stable
body:
```json
{
  "code": "FIX_RECORDED_AFTER_SESSION_ENDED",
  "retryable": false,
  "resubmit": "reacquire",
  "sessionId": "...",
  "endedAt": "...",
  "recordedAt": "..."
}
```

**THE QUEUE RULE:**
```
2xx                                  -> delete
retryable=false, resubmit=reacquire  -> RE-TAG and resend, do NOT delete
retryable=false, no resubmit path    -> delete and record permanent rejection
network / 408 / 429 / 5xx            -> keep
anything ambiguous                   -> keep
```

**BATCHES ARE PARTITIONED, NOT REJECTED.** Per-item outcomes:
```json
{
  "accepted": ["idem-key-before-end"],
  "rejected": [{ "idempotencyKey": "idem-key-after-end",
                 "code": "FIX_RECORDED_AFTER_SESSION_ENDED",
                 "retryable": false, "resubmit": "reacquire" }]
}
```

**FOUR REFINEMENTS:**

**1 - `retryable: false` MUST NOT MEAN "DELETE" HERE, BECAUSE ISSUE 17.13 WOULD
TURN IT INTO SILENT PERMANENT LOCATION LOSS.** The tracker never stops when the
user leaves the SOS screen, and `if (running) return` means a second activation
no-ops **keeping the FIRST session's `sessionId`**. So after a supersession the
client keeps capturing fixes tagged with the **ended** session, every one with
`recordedAt >= endedAt`. Under a naive delete rule the app looks like it is
tracking and nothing is recorded - **precisely the emergency case, because
supersession happens when a NEW incident starts.** Hence `resubmit: "reacquire"`.

**2 - `endedAt` CAN BE NULL. SPECIFY IT.** A null `endedAt` on an `ENDED` session
means reject-and-reacquire, never accept. **Also: `endedAt` is NOT currently
selected** at `:95`.

**3 - SEQUENCING. THE PER-ITEM BODY SILENTLY BREAKS THE CURRENT CLIENT.** Today
the client deletes on 2xx; a half-rejected batch returning 2xx would read as full
success. **Server and client must land in the same commit, or the server change
ships after the client learns to parse the body.**

**4 - THE IN-ORDER FLUSH INVARIANT, WHICH 9c MUST PRESERVE.** `893d65a` orders by
`receivedAt DESC, sequence DESC`, correct **only because the tracker flushes in
capture order, oldest first.** If 9c retries an old batch AFTER newer ones
succeeded, the newest `receivedAt` would carry the OLDEST `recordedAt` and the
tracking page would show a stale position as current. **Either 9c preserves
in-order flushing as an explicit invariant, or the latest-fix query moves to
`recordedAt`.**

**OPEN SCOPE QUESTION.** The skew checks at `:114-127` throw from inside `.map()`,
so one bad `recordedAt` 400s the whole batch - the same defect ADR-014 fixes for
ENDED, and the same shape as 17.6's negative-speed gap.
**Recommendation: one policy, applied to all.** **And note this is now the THIRD
member of that family, alongside the readiness verdict (decided in ADR-016) and
the `Promise.all` enrichment case (undecided).**

### 9b.10 ADR-013 - EVIDENCE ARCHITECTURE. CONTENT DECIDED, STILL OWED.

**UNCHANGED for two sessions.** Carried forward from Ultra 18.

**1. Priority ordering, and evidence must never delay location.**
```
location -> SOS/status messages -> telemetry -> audio -> photos -> video
```
A 200-fix batch is a few KB; thirty seconds of video is several MB. **On a weak
connection during an emergency, video would starve location updates - degrading a
proven capability to add an unproven one.** They use different endpoints, so the
priority queue is client-side scheduling across two transports.

**2. AUDIO ABOVE PHOTOS.** A phone in a purse produces twenty unusable photos;
a microphone in the same purse still captures speech.

**3. THE BINDING CONSTRAINT ON AUDIO IS BYSTANDER CONSENT, NOT USER CONSENT.**
The user cannot consent on behalf of the shopkeeper or passer-by whose voice ends
up in the file. **A question for counsel about a specific jurisdiction, not a
design toggle** - and the one item that genuinely blocks on NDPA. **An audio
recording is only evidence if it is admissible.**

**4. THREE INTEGRITY MECHANISMS, ONE TIMELINE.** Location fixes have a **hash
chain** (proves nothing was REMOVED); evidence has **per-object sha256** (proves
a file was not altered); **the timeline is chained too**. A chain is strictly
stronger. **Folding location into per-object evidence would WEAKEN it.**

**5. AI SUMMARIES ARE NOT EVIDENCE OBJECTS.** The first competent challenge
attacks the summary and discredits everything adjacent. Derived material belongs
in a separate, clearly-labelled layer that REFERENCES evidence.

**6. DEVELOPMENT AND DEPLOYMENT ARE SEPARABLE.** The pipeline can be built while
capture release waits on regulatory work - and most of it already exists.

**7. RESPONSE COORDINATION - DEFERRED, NOT REJECTED.** Independence is the only
structurally uncopyable advantage: **a responder cannot produce neutral evidence
about their own response.** **The hash chain does NOT rescue this** - tamper-
evidence is not independence. **The boundary while deferred: VIEWER, NEVER
CONSOLE.** Incident list, live position, report retrieval, acknowledgement
display - yes. Responder assignment, status tracking, escalation - no. **Stated
explicitly because a viewer drifts into a console one reasonable request at a
time.**

**8. PARTNER APIs COME AFTER PRODUCT-MARKET FIT** (`TODO.md:1335-1339`):
*pilot -> users -> reliability -> partners ask -> API.* The first realistic
integrators are **things that HAVE A TRIGGER BUT NO BACKEND**.

### 9b.11 ADR-015 IS STILL NOT INSTALLED

**`docs/handovers/ADR-015-content.txt` is committed. `decision-log.md` contains
no ADR-015.** **Re-confirmed this session by heading scan: `adr_count=13`, and
the thirteen are 001-012 plus 016.** Trap #95, third session running.

**ADR-015's content, summarised** - the full text is in the repo:

**OPA as a hardware and telemetry event middleware, with the mobile app as a
first-class client.** Status: Accepted in principle; implementation FROZEN until
its section 7 entry criteria are met.

Key decisions: a device-ingestion controller with **no `/api/v1` prefix**
(`main.ts` sets no global prefix and `redactSensitivePath` depends on that shape);
fixed devices carry an **install-time location**; **the Command Centre becomes
required**, still bounded by ADR-013's *viewer, never console*; **9c REMAINS
Tier-1**; **the hardware ownership model is DEFERRED**, with a "virtual system
user" explicitly REJECTED; enum changes **additive only**; device auth is a
**SECOND strategy alongside JWT**, with `UNIQUE(integrationPrincipalId,
externalEventId)` enforced in the database and `payloadVersion` required from the
first event.

**Section 6b is directly relevant to ADR-012:** *a notification provider must
never report a delivery it did not attempt.* **Chaining a claim does not make it
true.**

**SECTION 7'S ENTRY CRITERIA:**
| Criterion | Status |
|---|---|
| The production API boots and serves requests | **SATISFIED** |
| The schema is fully migrated | **SATISFIED - 11/11** |
| All required credentials present and production-grade | **PARTIAL - `REDIS_URL` is a deliberate stub** |
| Any exposed credential rotated | **SATISFIED and VERIFIED** |
| Notification providers comply with 6b | **UNVERIFIED** |
| Sprint 10B closed at 100% including 9c and the airplane-mode loop | **NOT MET - ~88%** |

**The freeze does not lift. 10B is the long pole.**

### 9b.12 THE DEVICE-DARK ANALYSIS - four states, one recoverable

**UNCHANGED. Carried forward.**

| State | Capture | Transmit | Trail |
|---|---|---|---|
| Location revoked | no | yes | ends |
| **Airplane mode** | **yes - GNSS is receive-only** | no | **delayed, not lost** |
| Force-killed | no | no | ends |
| Powered off | no | no | ends |

**Airplane mode is the case 9c changes**, and it is what item 10 exercises.
**THE GAP: going dark is NOT an attested event.** `SILENT` is derived at read
time from fix age - not persisted, not chained. **The chain is ready to carry
such an event; nothing writes one.**

**The one genuine software-only mitigation is SILENT ACTIVATION.**

---

## 10. The token leak - CLOSED. Historical record only.

Fixed in `7b152cd`. `global-exception.filter.ts` read `path: request.originalUrl`
raw in two places without importing `redactSensitivePath`. **The fix was FOUR
sites, not two** - the four tracking states never reach the exception filter
(the controller uses `@Res({ passthrough: true })`, zero `throw` statements
across all 8 files in `src/modules/incident-access`); what fires is Nest's
unmatched-route 404, whose message contains the full URL.

**OBSERVABLE IN PRODUCTION.** The Log stream shows that path firing for
`/robots933456.txt`, `/` and `/favicon.ico`.

**The durable lesson: when a handover explains WHY something is urgent, check the
mechanism, not just the conclusion.** **Seven recurrences.** **This session added
an eighth of the same family** - Ultra 18 explained why Redis was urgent, and the
mechanism turned out to be a deliberate stub with one consumer.

---

## 11. Production - LIVE. One semantic defect, now governed.

### Status - MEASURED THIS SESSION

| Fact | State | Evidence |
|---|---|---|
| API process | **running** | `/api/docs` 200 |
| External serving | **200, 3126 bytes** | first attempt, no cold-start delay |
| `/health` (liveness) | **200** | Azure probe path |
| `/health/ready` | **503** | measured, Nest-originated |
| Database, from the APP | **up** | `"database":"up"` from a live `SELECT 1` |
| Schema | **11 of 11** | Ultra 18 |
| Partial index | **correct** | Ultra 18 |
| Boot flag | `OPA_BOOT_WITH_SUPPRESSED_MOCKS=true` | Ultra 18 |
| `REDIS_URL` | **`redis://placeholder:6379`** | portal, Show value |
| Azure health probe path | **`/health`** | portal, Monitoring -> Health check |
| Unhealthy instance removal | **on, 10-minute threshold** | same blade |
| Write paths | **NEVER EXERCISED** | no incident has ever been created |

**`SELECT 1` IS A READ.** Issue 17.27 stands: no production incident, no
production fix, no production notification, no tracking token.

### The network model - UNCHANGED and permanent

**`opa-api-production-server` is Private access (VNet Integration). "Public
access" is greyed out and cannot be changed.**

1. **No route to the database from outside the VNet.**
2. **The only route is the App Service**, via Kudu SSH, **which requires a
   container that stays up** (trap #97). That condition is currently met.
3. **CI migration remains impractical.** Options: a self-hosted runner inside the
   VNet, or manual migration from the App Service. Open question 44.

**Log stream works without SSH and is the correct instrument for anything
boot-related.**

### The migration procedure, as executed in Ultra 18 - WRITE THIS INTO `docs/`

**Still not written down anywhere but a handover. ADR-016 D6 now gives it a
home: the permanent production-operations runbook.**

```
Azure Portal -> opa-api-production -> Development Tools -> SSH -> Go

printf '\e[?2004l'                                    # trap #98
cd /home/prisma-tools
ls node_modules/.bin/prisma && ./node_modules/.bin/prisma --version    # go/no-go
export DATABASE_URL='postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require'
echo "len=${#DATABASE_URL}"                           # expect 160
./node_modules/.bin/prisma migrate status --schema /home/site/wwwroot/prisma/schema.prisma; echo "exit=$?"
./node_modules/.bin/prisma migrate deploy --schema /home/site/wwwroot/prisma/schema.prisma; echo "exit=$?"
./node_modules/.bin/prisma migrate status --schema /home/site/wwwroot/prisma/schema.prisma; echo "exit=$?"
history -c
```

**Notes that are not obvious:**
- **`/home` is persistent App Service storage.** `/home/prisma-tools` survives
  restarts and redeploys. Everything outside `/home` does not.
- **`psql` is NOT installed.** Use the deployed Prisma client as a raw query
  runner, on ONE line.
- **`migrate status` exits 1 when migrations are pending.** Not a failure.
- **Do NOT accept the CLI's offer to upgrade to Prisma 7.**

**THE RUNBOOK MUST ALSO CARRY ADR-016 D6:** the App Service health probe path is
`/health` and must not be changed to `/health/ready` until every
readiness-critical dependency is provisioned and verified. **Check it during
deployment verification.**

### What is still owed on the production track

1. **Implement ADR-016 and verify `redis: "optional-down"` on the wire.**
2. **A CI test gate.** Two lines. **`main` currently carries a deliberately
   failing test, which makes the argument better than any hypothetical.**
3. **Gate Swagger, or decide deliberately not to.** Issue 17.8, live and
   unauthenticated.
4. **The Burstable tier.** Issue 17.24b. Azure's own banner recommends General
   Purpose or Memory Optimized for production.
5. **Write the migration procedure and the probe pin into a permanent runbook.**
6. **A controlled first production write** (q49).

**No infrastructure-as-code beyond the GitHub workflow. No Dockerfile.**
**"Booting locally is not deploying, deploying is not running, running is not
serving a real request"** - and now a fourth: **serving is not passing your
readiness probe.**

---

## 12. The document estate

### In the repo

| Lines | Path |
|---|---|
| 1599 | `docs/TODO.md` |
| **1210** | `docs/architecture/decision-log.md` |
| 623 | `docs/SPRINT_ROADMAP.md` |
| 469 | `docs/PARTNER_INTEGRATION_AND_DIFFERENTIATION.md` |
| 286 | `docs/PANIC_SYSTEM_CAPABILITY_ASSESSMENT.md` |
| 269 | `docs/VOICE_ACTIVATION_FEASIBILITY_STUDY.md` |
| 194 | `docs/LAUNCH_AND_REGISTRATION_ROADMAP.md` |
| 182 | `docs/architecture/emergency-intelligence-engine.md` |
| 166 | `docs/REGISTRATIONS.md` |
| 112 | `docs/architecture/system-overview.md` |
| 98 | `docs/architecture/incident-lifecycle.md` |
| 94 | `docs/future/ussd-fallback.md` |
| ~88 | `docs/COMMERCIAL_ROADMAP.md` |
| 138 | `docs/handovers/ADR-012-content.txt` |
| n/a | `docs/handovers/ADR-015-content.txt` **NOT INSTALLED** |
| n/a | `docs/handovers/OPA-Handover-Ultra-{13,15,16,17,18}-part{1,2,3}.txt` |

**A permanent production-operations runbook DOES NOT EXIST and is now owed by
ADR-016 D6.**

### THE ENCODING TRAP - three instances, two opposite causes

**`TODO.md` and `SPRINT_ROADMAP.md` carry a UTF-8 BOM and contain ZERO real
em-dashes.** Their mojibake is **literally in the files** - at `:352`, `:383`,
`:628`, `:642`, `:884`.

**`decision-log.md` is CLEAN - no BOM.** **Measured this session at
`bytes=49133` / `chars=49062` pre-insertion, a 71-byte delta = ~24 real
three-byte characters.** Its ADR-001..008 headings render as mojibake only
because `Get-Content` misreads UTF-8 as ANSI (trap #63). **ADR-009 through 016
use a plain hyphen and render normally.** **`incident-timeline.service.ts` is the
same case.**

**The cleanup applies to `TODO.md` and `SPRINT_ROADMAP.md` ONLY.**

**`bytes` minus `chars` is the cheap test, and it OUTLIVED the absolutes** -
Ultra 18's byte and char figures for `decision-log.md` were both stale while the
71-byte delta remained correct. **Pin the invariant, re-measure the absolutes.**

### Structural facts - RE-MEASURE, DO NOT TRUST

**`TODO.md`** uses `## ====` banner / title / `## ====`. The bare banner is NOT
unique - **anchor on a multi-line combination.** `### DONE` occurs **3 times**.

**Top-level banner map** (before the +75 at ~1491): `:8` Immediate, `:21`
Confirmed real, `:48` Known gaps, `:144` Journey Intelligence, `:190` Legal,
`:206` Backend, `:212` Deployment, `:222` App store, **`:228` Documentation
status**, `:242` A technique worth remembering, `:261` Where we stopped, `:285`
OPA Prevention, `:347` SOS deferred hardening, **`:352` Redis - READ THIS
SESSION, see part 1 section 3**, `:383` Website copy, `:418` Dispatch hardening,
`:606` Sequencing decision, `:628` Tomorrow's plan, `:642` Dispatch phase 2c,
`:754` Gap register, `:858` unbuilt features, `:884` Messaging channel
architecture, `:957` Pre-Sprint-10 integrity, `:1016` Sprint 10A architecture,
**`:1124` Sprint 10B progress**, **`:1318` STRATEGIC PRINCIPLES**, `:1411` Camera
integration. **Everything below ~1491 has shifted by +75.**

**The `:347` and `:352` banners were CONFIRMED at those exact positions this
session**, which is mild evidence the map above is still accurate.

**`SPRINT_ROADMAP.md`:** Sprint 10B at `:166` (`~87%`); Live tracking matrix row
at ~`:351`; Sprint 11 entry at `:223`. Feature Readiness Matrix near `:346`.

**Sprint and phase map:**
```
Sprint 10A Incident Portal       SUBSTANTIALLY BUILT
Sprint 10B Live Tracking         IN PROGRESS ~88%
Sprint 10C Location Intelligence CONFIRMED HARD BLOCKER - and LARGER than recorded
Sprint 11  Evidence Capture      SERVER-SIDE SUBSTANTIALLY BUILT
Sprint 12  User Profile          NOT STARTED - medical fields need a migration
Phase 3 / Sprint 13 Dashboard Foundation      NOT STARTED
Phase 3 / Sprint 14 Incident Management UI    NOT STARTED
Phases 4-9 and a parallel lettered track A-K
```
**The phase/sprint duality is itself a documentation hazard.**

### Standing practice - IN FORCE

> **Code commit first, gated on tests. Docs commit second, updating the Sprint
> 10B progress section with the step, its SHA, and its MEASURED gate.**

**Inverted deliberately five times now:** ADR-010 committed BEFORE its code,
ADR-011 AFTER, ADR-012 BEFORE its code, Ultra 18's amendments BEFORE the
implementation they described, and **this session's ADR-016 and its specs BOTH
before the implementation.**

**`194b32a`'s sequencing principle was followed explicitly: tests that pin
CURRENT behaviour land BEFORE the change**, so the change's gate is a comparison
rather than a co-written assertion. **`3e9b703` is that principle applied to a
change that has not happened yet.**

**Record gates that could actually have failed.** For `3e9b703` the honest gate
is that exactly one of five tests fails and it is the predicted one; for
`fc3d72c` it is a measured `209 0` numstat and a read-back proving written
content matched planned.

### Still outstanding from the estate sweep

- Undated relative markers at `TODO.md:190` and `:557`.
- The two competing "Phase A"s, and the phase/sprint duality.
- SafeWalk's incomplete gate list in `COMMERCIAL_ROADMAP` C60-63, C8-9.
- The "log the record id, never the raw token" checkbox - still UNVERIFIED.
- The mojibake/BOM cleanup, two files only.
- **`SPRINT_ROADMAP.md:136`** lists movement/compass as 10B scope, contradicting
  `:172-175` and ADR-009. **ADR-009 is right.**
- The two strategy docs are orphaned. `TODO.md:228` is the natural home.
- A Feature Readiness Matrix row for Sprint 11 / Evidence.
- An audit for schema-field-as-capability claims AND capabilities recorded as
  absent. **Now with three named starting points: `NotificationStatus.ACKNOWLEDGED`,
  `redisDispatchPrepared`, and `isVerified: true` on mock provider records.**
- **The four OLD-FLAG prose hits** at `TODO.md:984, 993` and
  `SPRINT_ROADMAP.md:210, 396`. **Factually wrong, not merely stale.**
- **Anywhere either document says production is down or "one code change from
  booting."**
- **NEW - `TODO.md:366-368`'s Redis polish item describes an `ECONNREFUSED`
  symptom; production shows `ENOTFOUND`.** The item is written for a problem this
  deployment does not have.
- **NEW - `TODO.md:362-363` says to verify `/health/ready` shows `redis: up`
  after provisioning.** Under ADR-016 the target state with Redis deferred is
  `redis: optional-down` and HTTP 200. **That line needs updating with the
  implementation.**

### `docs/future/ussd-fallback.md` IS CONVERSION-DAMAGED

94 lines, BOM + CRLF, `bytes=1870` vs `chars=1846`. Escaped markdown, `&#x20;`
entities, a blank line between every content line - a Word-to-Markdown round
trip. **Its `## Fallback decision tree` heading at line 21 is followed
immediately by the next heading; the decision tree is MISSING.** **It was born
damaged** - one commit, `c427571`, blob identical to the working copy. **Git
cannot recover it.**

Content worth keeping: USSD needs cellular signal, a supported network, **a
registered short code via a telecom or aggregator requiring formal approval**,
and a session capped around 2-3 minutes. **Never transmit evidence, medical
records or long location payloads over USSD.** **Its roadmap places USSD LAST**,
gated on a partnership OPA does not control.

### Downloads / DocStaging

42 `.docx` converted into `C:\Projects\OPA-DocStaging` plus `_INDEX.md`,
**deliberately outside the repo**. **But measured: `docx_count=201` in Downloads,
and DocStaging contains NO `.docx` at all.**

**Two divergent-version hazards, both trap #38:**
- `OPA-Master-Roadmap-v2.docx` **35,486 bytes** vs its `(1)` copy **35,776**.
  The converted `.md` came from **v1** (27,489), so ~8KB of v2 was never
  converted.
- `OPA_Journey_Risk_Intelligence_Architecture_v1.0.docx` **520,882 bytes** (7/26)
  vs its `(1)` copy **393,632** (7/14). **Not duplicates.** This is the 2592-line
  document that **has never been opened in any session**, in two materially
  different versions.

**Filing plan:** `docs/` engineering only; `docs/business/` positioning; **outside
the repo entirely** for the offer letter, hiring structure, CAC certificate,
letterheads, sender-ID letters.

**Two marketing documents produced, NOT committed:**
`OPA-vs-Panic-Buttons-Briefing.docx` and `OPA-Staff-Guide.docx`.
**THE STAFF GUIDE NEEDS TWO CORRECTIONS.** It says location is captured at the
moment of the press - **false since 29 July**. **And if it or the briefing claims
background recording or encrypted evidence, both are false.**

---

## 13. Strategy

**UNCHANGED THIS SESSION except where noted.**

### The capability assessment (286 lines)

- **Provenance matters:** the source checklist is vendor-authored. **Market
  intelligence, not a neutral standard.**
- **Names the gap the checklist missed: OPA is foreground-only.**
- **THE BLOCKING FACT for indoor positioning:** proposed indoor fields cannot
  enter the canonical fix payload without breaking verification for **every fix
  already stored**. **q32 - three cases.**
- **Rejects** in-app VoIP and silent-listening. **Accepts** responder call-back
  with number masking.
- **Guardian gets no spec** - which is why `JourneyPurpose.GUARDIAN` is excluded
  from `CLIENT_PURPOSES`. Its 2592-line architecture document **has never been
  opened.**

### The partner/differentiation document (469 lines)

- **Names a competitor absent from every prior strategy document: the phone
  itself.** Apple Emergency SOS with satellite, crash detection, Android Personal
  Safety - free, pre-installed, deeper OS access.
- **Ranks differentiators by defensibility.** Defensible: **independence of the
  record**, and **Nigerian channel and regulatory fit**. **NOT a differentiator:
  the hash chain.** **The mechanism is not the advantage.**
- **The differentiator only monetises in the institutional channel.** Consumers
  want rescue; institutions want proof. **OPA cannot provide rescue and can
  provide proof.**
- **QUALIFIED BY `TODO.md:1335-1339`:** partner APIs come after product-market
  fit. **ADR-015 knowingly inverts this and says so.**

### Command Centre - scoping discussed, not decided

**It is a WEB dashboard, not a mobile app.** The public tracking page is the
precedent: web-only, no download, no login, reached by a tokenized SMS link.

**Four surfaces conceptually; three built.** The Command Centre **does not exist
at all**.

**KEEP THE ADR-013 BOUNDARY: viewer, never console.**

**THE UNANSWERED QUESTION THAT CHANGES THE DESIGN: is the institution buying for
its STAFF or for its CLIENTS?** **ADR-015 shifts the answer toward neither: the
partner's DEVICES are enrolled, not people.**

**Zero-download triggering is NOT possible today** - `sos.tsx` is the only
trigger. For hospitals the real zero-download trigger is **hardware**, whose
fixed known location solves the required `latitude`/`longitude` trivially.

### Outreach - the honest claim list

A drafted estate email claimed an active pilot, an emergency command centre
platform, zero app downloads to trigger, a security-team dashboard mapping
responders and sharing data with emergency services, and **full NDPA
compliance**. **Every one is still false**, and the dashboard claim is precisely
the coordination drift ADR-013 warns against. **SHOULD NOT BE SENT AS WRITTEN.**

**What is true and worth leading with:** tokenized SMS links opening a live map in
any browser with no download or login; a device-verified ten-second cadence; and a
tamper-evident record - location fixes and timeline events both chained, with a
verifier.

**TRUE AND NARROW, stated carefully:** the backend is deployed and running in
Azure with a fully migrated schema, **and the deployed application has executed a
query against the production database.** **That is an infrastructure fact, not a
product claim.** **It does NOT mean the system has handled a real incident** - no
production write path has ever executed. **Do not let "production is live" drift
into "we have a working service in the field."**

**Recommended framing: pre-pilot, seeking one or two design partners.**

**Two things to fix before any demo:** render the tracking page, and the
`contactsNotified` "0 contacts" bug.

### THE DEMO-CRITICAL PATH - REASONED THROUGH THIS SESSION

**The B2B direction holds, and the documents support it.** It also survives the
competitor the strategy docs named late - the phone itself (Apple Emergency SOS
with satellite, crash detection, Android Personal Safety: free, pre-installed,
deeper OS access). **You cannot out-consumer that. Apple does not produce an
admissible, independent record for a hospital's incident file.**

**But the tension is real: B2B is right AND nothing is demonstrable.** The
tracking page has never rendered. No production write path has ever executed.
`contactsNotified` shows 0. **An institutional buyer asks to see it and there is
nothing to show.**

**The demo workflow is a SLICE ACROSS FOUR SPRINTS, not a Sprint 10 item:**

| Stage | Sprint | State |
|---|---|---|
| Incident creation | 9 | built, live-tested, four activations |
| Notifications | 9 / dispatch hardening | built, WhatsApp confirmed - **but the count displays 0** |
| Tracking page | 10A | built server-side, **never rendered** |
| Incident timeline | pre-10 integrity work | built, chained, verifier exists |
| **Evidence capture** | **11** | **client-side UNSTARTED, ADR-013 unwritten** |

**Sprint 10B - the actually open sprint - is NOT in that list.** 9c and item 10
make tracking survive airplane mode; they are not what a demo shows.

**So the honest demo, one hour of work away and running against LOCAL:** SOS on a
real phone -> incident created -> SMS and WhatsApp arrive with correct contact
counts -> tokenized link opens a live map in any browser, no download, no login
-> position updating at a verified ten-second cadence -> a tamper-evident chain
with a verifier behind it. **Evidence capture is not part of it, and claiming it
would repeat the estate-email problem.**

**THE WHITE PAPER IS UN-GATED AND CHEAP** - no procurement, no device, no 10C.
**Two constraints on it:** its strongest claim, a "device went dark" attested
event, **is not built** (open question 41 - the chain is ready, nothing writes
one), and **"immutable" is rejected vocabulary** (ADR-015). Tamper-evident.

**THE QUESTION THAT DECIDES WHETHER B2B IS A DIRECTION OR A DESCRIPTION:**
`TODO.md:1335-1339` sequences partner APIs AFTER product-market fit, and ADR-015
knowingly inverts that, mitigating by building for ONE NAMED PARTNER as a design
partnership rather than a public SDK. **Is there a named partner? Hospital
outreach is drafted and not sent - the oldest open action item.**

### Older business documents - outside the repo, still drifting

`OPA-Product-Family-Positioning` (7/18), `OPA-Launch-Sequencing-Final` (7/18),
`OPA-Strategic-Blueprint` (7/26),
`OPA_Journey_Risk_Intelligence_Architecture_v1.0` (7/26, 2592 lines, **never
opened**).

**Key conflict:** launch sequencing ranks SafeWalk 3rd and Enterprise 4th;
`COMMERCIAL_ROADMAP` makes Command Center Release 1 and SafeWalk Release 2.
**But coordination is deferred, so Command Center's cheapness is no longer a
reason to build it.**

**Two claims to verify before either reaches an investor:** ERA's "$20K seed"
looks like a Techstars standard cheque; the Nigerian healthtech figures (~$55M
2023 -> ~$18M 2024 -> ~$3M 2025) are load-bearing.

---

## 14. Wider project context

**UNCHANGED THIS SESSION.**

### Registration - THE CLOCK IS RUNNING

- **CAC DONE.** RC 9697630, OPA Technologies Limited.
- **D-U-N-S IN HAND. 352294843.** Apple and Google Organization enrollment
  unblocked, **pending a 2-3 day D&B activation window.**
- **START THE SLOW PARTS NOW** - elapsed time, not effort.
- **The free D&B self-service tools are documented as being for U.S.-based
  non-public companies.** A Nigerian entity likely needs the regional **Data
  Update** route. **Whoever submits must be an owner, director or officer.**
- **The fields that decide developer-program verification are legal name, HQ
  address and phone** - byte-identical to CAC and the website.
- **NDPA registration is IN PROCESS.** **The constraint on audio capture is
  timing, not permission.**
- **Hospital outreach: drafted, not sent. The oldest open action item.**

### Sprint status

- **Sprint 9:** Pass 1 done, live-tested; WhatsApp confirmed across four
  activations. **Error paths appear BUILT BUT UNTESTED**, plus two named
  defects. **`contactsNotified` shows 0.**
- **Sprint 10A:** built, locally verified. **The tracking page has still never
  rendered**, and 54 real fixes exist locally. **Three sessions.**
- **Dispatch hardening:** complete, `e7990e2`.
- **Sprint 10C:** hard blocker. Procurement **and a from-scratch integration
  layer** - see 9b.5.
- **Sprint 11:** server-side substantially built. **`EvidenceModule` confirmed
  initialising in production.**
- **`docs/REGISTRATIONS.md`** was found silently truncated once and rebuilt.
  **If any doc looks shorter than expected, verify its line count.**

### Voice activation - PICOVOICE STATUS UNCERTAIN

269-line study. Desk research complete; device testing outstanding. L1/L2
feasible on both platforms; L3 feasible on Android, iOS conditional. **Endorsed:
Android-led voice, two-tier model.** Privacy framing: on-device keyword spotting,
no cloud audio, OS mic indicator visible. **Never imply hidden or silent
listening.**

**Corrected GO/NO-GO: measurement prototype GO now; ship as a product feature NOT
YET; publish marketing claims NOT YET.** **Porcupine's performance targets are
ESTIMATES, NOT VERIFIED.**

**The user stated Picovoice is "not ready". Confirm before spending time.**
**BLOCKER: Expo Go cannot run Porcupine.** Custom dev build -> EAS -> **no
`eas.json`**. Backend is ready - `IncidentTrigger.VOICE_HELP_HELP` and
`EmergencyTriggerType.VOICE` exist.

### Six product ideas. First three nearly free.

1. **Contact acknowledgement.** `ACKNOWLEDGED` exists and NOTHING SETS IT.
   Opening the tracking page already calls `recordAccess()`. **The largest
   functional gap. The fix for the "0 contacts" bug and this idea are the same
   work.** **It proves response time WITHOUT OPA participating in the response.**
2. **"I am responding"** on the tracking page - the bystander-effect fix.
3. **Surface battery.** `batteryLevel`/`isCharging` stored and hashed, **and the
   app has never captured either.** **Free telemetry.** **The DTO says 0-100.**
4. **Duress cancel.** `handleCancel` never calls the API.
5. **Hardware trigger** (triple power press / held volume-down) - **80% of
   wake-word for 10% of the cost, no mic, no App Store risk, no NDPA audio
   question. BEFORE voice. This is the actual silent-activation mechanism.**
6. **Automated incident report on resolution.** The OUTPUT of everything 10B
   built, and **the sellable artifact in the partnership model.**

### Silent activation - and the unasked design question

**Backend ready. App NOT silent** - five-second visible countdown, full-screen
state changes, large button. **The mechanism is product idea 5.**

**THE DESIGN QUESTION NOBODY HAS ASKED: if activation is silent, how does the
user know it worked?** Screen confirmation defeats the purpose. **A single
discreet haptic is the likely answer. Decide before building.** **It interacts
with the `contactsNotified` defect: a user who cannot look at the screen has no
confirmation channel at all.**

### Positioning - "live" is EARNED, "background" is NOT

**Rejected: "emergency coordination platform."** **Adopted:** *emergency alerting
with a verifiable location record* / *live incident awareness*.

**"Live" is earned as of `d5aca8b`** - 50 fixes, 10s cadence, verified chain.
**Caveat: live WHILE THE APP IS IN THE FOREGROUND.** `source: 'background'` is
unreachable and the string is absent from the client entirely. **Do not let
"live" grow into "always".**

**HONEST CAPABILITY STATEMENT:** *records continuously while the app is open, at
a verified 10-second cadence, on a tamper-evident chain.*

**A SECOND HONEST CLAIM:** *incident events are recorded on a hash chain with a
verifier, and writes for one incident are serialised in the database.*

**A THIRD, NARROW:** *the backend is deployed and running in Azure against a
fully migrated schema, and the deployed application has executed a query against
the production database.* **Infrastructure, not capability.**

**Two words to avoid** (ADR-015): **"immutable"** - the chains are
**tamper-evident**; and **on-device Picovoice processing is NOT an existing
asset**.

### Website

Next.js 16.2.10 on Vercel, **no tests**, gated on `npm run build`. 18 `.tsx`
files, **exactly ONE `'use client'` component**. **`middleware` is deprecated in
Next 16.2 in favour of `proxy`.**

`src/lib/tracking.ts` **casts** the API response rather than validating it, so
**the website's types are a hand-mirrored copy of the API DTO with nothing
keeping them in sync.** One drift exists (q20).

---
